(function() {

  angular.module('app', []) // Define module
    .controllers = {}; // Custom object container for controllers
}()); // Self-invoked anonymous function; prevents polluting global namespace
